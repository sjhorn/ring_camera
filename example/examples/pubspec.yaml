name: examples
description: Ring Camera API examples - demonstrates various features similar to TypeScript examples
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  ring_camera:
    path: ../../
  ring_client_api: ^0.1.0
  flutter_dotenv: ^5.2.1
  path_provider: ^2.1.5
  path: ^1.9.0
  logging: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/audio/
